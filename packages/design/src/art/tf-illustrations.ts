import { ILLUSTRATION_SVG } from '../illustrations'

/** The Open Peeps, as the pack shape. Its own module so a product that reuses
 *  tf's icons and not its figures does not carry 240 KB of them. */
export const TF_ILLUSTRATIONS = ILLUSTRATION_SVG
