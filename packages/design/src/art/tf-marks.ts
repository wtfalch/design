import { BRAND_MARKS } from '../components/brandMarks'

/** tf's mark, as the pack shape. The path itself stays in `brandMarks.ts`,
 *  which tf's own test reads out of the compiled package. */
export const TF_MARKS = BRAND_MARKS
